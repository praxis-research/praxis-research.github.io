{
  "name": "praxis-website",
  "version": "1.0.0",
  "private": true,
  "description": "praxis-research.org \u2014 a static site with no framework.",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "serve": "node build.mjs && node serve.mjs",
    "check": "node build.mjs --check",
    "deploy": "node deploy.mjs",
    "preview": "node build.mjs && node preview.mjs",
    "add-note": "node bin/port-artifact.mjs"
  },
  "dependencies": {
    "marked": "^15.0.7"
  }
}
